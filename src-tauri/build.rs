fn main() {
    // It's recommended to use the built-in `tauri-build` helper.
    // This will automatically set up the correct environment variables
    // for your Tauri application.
    tauri_build::build()
}
